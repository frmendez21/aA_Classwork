<h1>All Of Our Cats</h1>

<ul>
    <% @cats.each do |cat| %>
        <li><a href="<%= cat_url(cat) %>"><%= cat.name %></a></li>
        <br>
    <% end %>
</ul>

<a href="<%= new_cat_url %>">Add New Cat!</a>
